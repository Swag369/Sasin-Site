import './Footer.css';
import 'bootstrap/dist/css/bootstrap.min.css';

const Footer = () => {
    return (
		<>
		<div style={{display: "flex", justifyContent: "center", alignItems:"center", fontSize: "12px", textAlign: "center"}} className="footer">
			Resume in Nav Bar (Next to Instagram Icon) Make that sound and look better
		</div>
		</>
    )
}

export default Footer;